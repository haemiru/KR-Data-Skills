# 인증키 — 절대 커밋 금지
.env
*.env
!.env.example

# 라이선스 고지 기록 (워크스페이스 로컬 상태)
.licenses/

# 작업 문서 — 로컬 전용, 공개 저장소에 올리지 않는다
#
# NEXT.md(재개 문서)와 WORKLOG.md(결정 근거)는 유지보수자용이다.
# 스킬을 가져다 쓰는 사람에게는 노이즈고, 무엇보다 **다른 프로젝트의
# 경로·인프라·시크릿 이름이 새는 자리**다. 실제로 한 번 샜다.
# 공개 저장소에는 README.md 와 skills/ 만 있으면 된다.
docs/

# API 응답 산출물
/out/
*.response.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 도구 상태
.bkit/
.claude/

# OS
Thumbs.db
desktop.ini
.DS_Store
