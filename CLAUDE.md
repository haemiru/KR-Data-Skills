# CLAUDE.md — KR-Data-Skills

한국 공공데이터 API를 AI 에이전트용 스킬로 감싼 오픈소스 팩.
이 파일은 **이 저장소에서 작업할 때의 규약**이다. 스킬을 가져다 쓰기만 할 거면
`README.md`만 보면 된다.

## 범위

**이 저장소는 스킬 팩만 다룬다.** 그 위에 올릴 업종별 서비스는 별도 프로젝트다.
여기에 만들지 말 것.

## 🔴 공개 저장소다 — 푸시 전 확인

원격은 `https://github.com/haemiru/KR-Data-Skills` (**공개**).

- **`.env`는 저장소 루트에 둔다.** 홈(`~/.env`)이 아니다. 래퍼는 현재
  디렉터리에서 `.git`이 있는 곳까지 거슬러 올라가며 찾으므로,
  `skills/<name>/` 안에서 실행해도 루트의 `.env`를 읽는다
- `.env`는 절대 커밋하지 않는다(`.gitignore`에 있음. `.env.example`만 커밋).
  푸시 전 `git ls-files`로 한 번 더 확인할 것
- **다른 프로젝트의 이름·경로·인프라·시크릿 변수명을 문서에 쓰지 말 것.**
  실제로 한 번 샜다 — 인증키 재발급 영향 범위를 정리하다가 비공개 저장소의
  존재와 시크릿 이름을 공개 문서에 적었고, 그대로 푸시됐다.
  그런 내용은 `docs/`(gitignore 대상)에만 쓴다
- 작업 문서(`docs/NEXT.md`, `docs/WORKLOG.md`)는 **로컬 전용**이다.
  공개 저장소에는 `README.md`와 `skills/`만 올라간다

## 새 스킬을 추가할 때 — 반드시 지킬 4가지

기존 `skills/g2b-bid-notice`를 그대로 베끼는 게 가장 빠르다.

1. **`--output` 필수** — API 응답을 컨텍스트에 쏟지 않는다. 파일로 쓰고
   필요한 부분만 읽는다. `--max-items`로 잘리면 `_meta.truncated`로 알린다
2. **라이선스 게이트** — `.licenses/<skill>_LICENSE.txt`가 없으면 약관 고지 후
   문구+타임스탬프를 기록하고 진행. SKILL.md 사전조건에 명시한다
3. **인증키 값 비노출** — `grep -sq`로 존재만 확인. 에러 메시지 마스킹까지 구현.
   `cat .env` / `echo $KEY` 절대 금지
4. **MCP가 아니라 CLI 래퍼** — 토큰 효율 때문에 의도적으로 택한 구조다

### 구조

```
skills/<kebab-name>/          # 폴더명 = frontmatter의 name (Claude Code 설치용)
  SKILL.md                    # YAML frontmatter + 에이전트 지침
  scripts/*.py                # uv 인라인 메타데이터, 표준 라이브러리만
  references/*.md             # 선택. 스키마·필드·함정 기록
```

- **data.go.kr 계열은 `DATA_GO_KR_SERVICE_KEY` 하나를 공유한다.** 스킬마다
  다른 키 이름을 만들지 말 것. 포털 인증키는 계정당 하나이고 모든
  활용신청 API에 통한다
- 외부 의존성은 되도록 0. 네트워크 막힌 환경에서도 돌아야 한다
- SKILL.md의 `description`에 **사용자가 실제로 쓸 한국어 표현**을 넣는다.
  그게 스킬 자동 호출의 트리거다

### 결과를 사용자에게 보고하기 전에

`g2b-bid-notice`에서 실측으로 배운 것들이다. 새 스킬에도 같은 함정이 있는지 볼 것.

- **응답에 없는 필드로 필터가 될 수 있다.** 나라장터는 지역·업종으로 필터링은
  되는데 응답에 그 필드가 없다. 근거를 못 붙인다 → 보조 오퍼레이션 조인 필요
- **행 유일 키가 ID 하나가 아닐 수 있다.** 나라장터는 (공고번호, 차수)다
- **필드 집합이 하위 구분마다 다르다.** 같은 `--fields`를 재사용하면 조용히 빠진다
- 잘림·불완전한 조인은 **시끄럽게 알린다.** 조용한 불완전이 가장 위험하다

## 개발 환경 주의 (Windows 기준)

- **`jq`를 전제하지 말 것.** 문서 예제는
  `uv run python -c "import json; ..."`로 쓴다
- **cp949 한글 깨짐** — 스크립트에서 stdout을 utf-8로 재설정하고,
  파일 입출력에 `encoding="utf-8"`을 명시한다.
  즉석 스크립트는 보호받지 못하므로 `-X utf8`과
  `sys.stdout.reconfigure(encoding="utf-8")`을 붙일 것
- `uv`가 스크립트에 맞는 파이썬 버전을 알아서 잡는다. 시스템 버전과 무관
- **환경에 따라 포트 80(http)이 막혀 있다.** 모든 API 호출은 `https://`로

## 검증 습관

인증키 없이도 확인할 수 있는 것이 많다. 추측으로 문서를 쓰지 말 것.

- data.go.kr은 **라우팅이 인증보다 먼저**다. 더미 키로 호출하면
  `code 30`(경로 유효·키만 틀림) vs `code 12`(경로 없음)로 갈린다.
  `probe-endpoints`가 이걸 쓴다
- **확인한 것과 추정한 것을 문서에서 구분한다.** 미검증이면 미검증이라고 쓴다
- **관계식으로 교차검증한다.** 나라장터에서 `bssamt / presmptPrce`가 전 건
  정확히 1.100인 것이, 조인이 올바른 행을 붙였다는 증거가 됐다.
  엉뚱한 행이 붙었다면 비율이 흩어진다
